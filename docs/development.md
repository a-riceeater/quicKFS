# Development

Run `./scripts/dev-cert.sh`, create an export directory, and use the commands in the root README. `RUST_LOG=quickfs=debug` enables structured diagnostics. `./scripts/check.sh` runs the required local quality gates.

