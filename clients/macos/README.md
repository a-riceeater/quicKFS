# macOS client

`client-cli` exercises the server without mounting. `filesystem-macfuse` defines the read-only adapter boundary, but native callbacks and the `macos-client mount` executable remain planned. Install macFUSE manually only when working on that integration; the project never installs system extensions.

